{
  "buildCommand": "npm install --no-audit --no-fund && npm run build && echo --- DIST LISTING --- && ls -R dist || dir dist",
  "outputDirectory": "dist",
  "framework": "vite",
  "env": {
    "NODE_ENV": "production",
    "VITE_APP_MODE": "production"
  },
  "ignoreCommand": "git diff --quiet HEAD^ HEAD || echo 'Changes detected'"
}